/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { CreatereportfromtemplateService } from './createreportfromtemplate.service';

describe('Service: Createreportfromtemplate', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [CreatereportfromtemplateService]
    });
  });

  it('should ...', inject([CreatereportfromtemplateService], (service: CreatereportfromtemplateService) => {
    expect(service).toBeTruthy();
  }));
});
