/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { CreatereportforsgxService } from './createreportforsgx.service';

describe('Service: Createreportforsgx', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [CreatereportforsgxService]
    });
  });

  it('should ...', inject([CreatereportforsgxService], (service: CreatereportforsgxService) => {
    expect(service).toBeTruthy();
  }));
});
