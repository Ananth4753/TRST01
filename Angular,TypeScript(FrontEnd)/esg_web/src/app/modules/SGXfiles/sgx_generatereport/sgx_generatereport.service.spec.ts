/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { Sgx_generatereportService } from './sgx_generatereport.service';

describe('Service: Sgx_generatereport', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [Sgx_generatereportService]
    });
  });

  it('should ...', inject([Sgx_generatereportService], (service: Sgx_generatereportService) => {
    expect(service).toBeTruthy();
  }));
});
