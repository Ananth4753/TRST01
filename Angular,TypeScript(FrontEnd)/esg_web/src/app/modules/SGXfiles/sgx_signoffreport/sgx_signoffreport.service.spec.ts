/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { Sgx_signoffreportService } from './sgx_signoffreport.service';

describe('Service: Sgx_signoffreport', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [Sgx_signoffreportService]
    });
  });

  it('should ...', inject([Sgx_signoffreportService], (service: Sgx_signoffreportService) => {
    expect(service).toBeTruthy();
  }));
});
