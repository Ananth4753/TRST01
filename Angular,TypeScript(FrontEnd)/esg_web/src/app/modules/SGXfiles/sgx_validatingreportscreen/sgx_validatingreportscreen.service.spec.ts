/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { Sgx_validatingreportscreenService } from './sgx_validatingreportscreen.service';

describe('Service: Sgx_validatingreportscreen', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [Sgx_validatingreportscreenService]
    });
  });

  it('should ...', inject([Sgx_validatingreportscreenService], (service: Sgx_validatingreportscreenService) => {
    expect(service).toBeTruthy();
  }));
});
