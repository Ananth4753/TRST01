/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { Brsr_validateService } from './brsr_validate.service';

describe('Service: Brsr_validate', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [Brsr_validateService]
    });
  });

  it('should ...', inject([Brsr_validateService], (service: Brsr_validateService) => {
    expect(service).toBeTruthy();
  }));
});
