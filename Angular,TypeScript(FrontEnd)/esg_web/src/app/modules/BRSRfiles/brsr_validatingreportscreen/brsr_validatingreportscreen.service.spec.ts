/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { Brsr_validatingreportscreenService } from './brsr_validatingreportscreen.service';

describe('Service: Brsr_validatingreportscreen', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [Brsr_validatingreportscreenService]
    });
  });

  it('should ...', inject([Brsr_validatingreportscreenService], (service: Brsr_validatingreportscreenService) => {
    expect(service).toBeTruthy();
  }));
});
