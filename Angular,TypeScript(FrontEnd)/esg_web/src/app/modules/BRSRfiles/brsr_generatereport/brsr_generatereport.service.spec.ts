/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { Brsr_generatereportService } from './brsr_generatereport.service';

describe('Service: Brsr_generatereport', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [Brsr_generatereportService]
    });
  });

  it('should ...', inject([Brsr_generatereportService], (service: Brsr_generatereportService) => {
    expect(service).toBeTruthy();
  }));
});
