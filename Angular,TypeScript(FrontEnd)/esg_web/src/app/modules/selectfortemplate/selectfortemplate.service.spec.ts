/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { SelectfortemplateService } from './selectfortemplate.service';

describe('Service: Selectfortemplate', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [SelectfortemplateService]
    });
  });

  it('should ...', inject([SelectfortemplateService], (service: SelectfortemplateService) => {
    expect(service).toBeTruthy();
  }));
});
