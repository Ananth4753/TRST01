/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { ReportcollaboratorsService } from './reportcollaborators.service';

describe('Service: Reportcollaborators', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [ReportcollaboratorsService]
    });
  });

  it('should ...', inject([ReportcollaboratorsService], (service: ReportcollaboratorsService) => {
    expect(service).toBeTruthy();
  }));
});
